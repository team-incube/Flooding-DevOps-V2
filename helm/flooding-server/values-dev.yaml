# Develop overrides — managed by CD for dev/develop branch
image:
  tag: "develop-5a2aca8"

replicaCount: 1

ingress:
  host: dev.flooding.kr
  extraHosts: []
  entryPoints:
    - dev
  tls:
    secretName: dev-flooding-kr-tls

resources:
  requests:
    cpu: "100m"
    memory: "256Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"

autoscaling:
  enabled: false

env:
  SPRING_PROFILES_ACTIVE: dev

livenessProbe:
  port: 8080
  initialDelaySeconds: 90
  periodSeconds: 15
  failureThreshold: 3

readinessProbe:
  port: 8080
  initialDelaySeconds: 60
  periodSeconds: 10
  failureThreshold: 3
