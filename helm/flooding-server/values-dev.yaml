# Develop overrides — managed by CD for dev/develop branch
image:
  tag: "develop-5c0c0e2"

replicaCount: 1

ingress:
  host: gsmsv-1.yujun.kr
  entryPoints:
    - dev

resources:
  requests:
    cpu: "100m"
    memory: "256Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"

autoscaling:
  enabled: false

env:
  SPRING_PROFILES_ACTIVE: dev
