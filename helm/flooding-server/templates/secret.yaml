# This template references an EXTERNALLY managed Kubernetes Secret.
# The secret must be created manually or via Ansible/external-secrets operator
# before deploying this chart.
#
# Required secret keys (create with):
#   kubectl create secret generic flooding-server-secret \
#     --from-literal=JWT_SECRET=<value> \
#     --from-literal=DB_URL=<value> \
#     --from-literal=DB_USERNAME=<value> \
#     --from-literal=DB_PASSWORD=<value> \
#     -n flooding
#
# This file is intentionally left as a reference — secrets are NOT stored here.
