apiVersion: v1
kind: Service
metadata:
  name: {{ include "flooding-server.fullname" . }}
  labels:
    {{- include "flooding-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flooding-server.selectorLabels" . | nindent 4 }}
